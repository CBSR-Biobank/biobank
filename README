MySQL SERVER
  - my.cnf must have "lower_case_table_names=1" in the [mysqld] section

INSTALL GETTEXT
  Gettext is used for i18n, it must be installed.

  sudo apt-get install gettext

BUILD ECLIPSE PROJECTS

  mvn eclipse:eclipse

  To also download available sources for libraries, instead run:

  mvn eclipse:eclipse -DdownloadSources=true

  
RUNNING

  WITH MAVEN
    From the biobank-webapp project use the command:
  
      mvn exec:java -Dexec.mainClass=edu.ualberta.med.biobank.BiobankServer  
      
  SHADED JAR
    From the biobank-webapp project use the commands:
    
        mvn package java -jar target/biobank-webapp-4.0-SNAPSHOT.jar
        